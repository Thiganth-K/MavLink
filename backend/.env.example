# Example environment variables for backend
# Copy this to `.env.development` and `.env.production` and fill values appropriately

# MongoDB connection string
MONGO_URI=mongodb://localhost:27017/your_db_name

# Server port
PORT=5001

# Super admin credentials (use secure storage in production)
SUPER_ADMIN_USERNAME=superadmin
SUPER_ADMIN_PASSWORD=change_me

# Origin to allow CORS (frontend URL)
FRONTEND_ORIGIN=http://localhost:5173

# NODE_ENV will be set by the start scripts; you can override it if needed
# NODE_ENV=development
